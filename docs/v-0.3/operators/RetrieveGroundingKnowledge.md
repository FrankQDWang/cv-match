# RetrieveGroundingKnowledge

已从当前 `HEAD` 移除。

当前 bootstrap 不再做 card retrieval。

替代物是：

- [[RouteDomainKnowledgePack]]
- [[DomainKnowledgePack]]
- [[BootstrapRoutingResult]]

当前运行时只会选中一个 knowledge pack，或者进入 `generic_fallback`。
